# Roblox-Username-Checker
Simple Python script to check list of Usernames via the public Roblox API.

Put the script inside an empty folder, create 2 other .txt files in there and set the names to the one in line 4 / 5.